{
  "name": "baby-app-rules-tests",
  "description": "Emulator-backed unit tests for firestore.rules",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project=demo-baby-app --only firestore --config ../firebase.json \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "firebase-tools": "^13.29.1"
  }
}
